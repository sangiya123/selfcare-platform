server:
  port: ${SERVER_PORT:8094}
  reactive:
    max-memory: 512M

spring:
  application:
    name: ai-gateway
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/selfcare_identity?useSSL=false&allowPublicKeyRetrieval=true}
    username: ${SPRING_DATASOURCE_USERNAME:selfcare}
    password: ${SPRING_DATASOURCE_PASSWORD:Selfcare_My5ql_Db_Pa55w0rd!2026}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  mongodb:
    uri: ${MONGO_URI:mongodb://selfcare:Selfcare_M0ng0_Db_Pa55w0rd!2026@localhost:27017/selfcare_config?authSource=admin}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:Selfcare_R3d1s_Pa55w0rd!2026}
  cloud:
    stream:
      kafka:
        binder:
          brokers: ${KAFKA_BROKERS:localhost:9092}
  threads:
    virtual:
      enabled: true

# Resilience4j
resilience4j:
  circuitbreaker:
    instances:
      ai.gateway.chat:
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
        permitted-number-of-calls-in-half-open-state: 5
        sliding-window-size: 10
  timelimiter:
    instances:
      ai.gateway.chat:
        timeout: 30s
        cancel-running-future: true
  retry:
    instances:
      ai.gateway.chat:
        max-attempts: 2
        wait-duration: 500ms

# selfcare AI configuration
selfcare:
  ai:
    # Default LLM provider
    default-provider: ${AI_DEFAULT_PROVIDER:anthropic}

    # Provider-specific config
    providers:
      anthropic:
        api-key: ${ANTHROPIC_API_KEY:}
        model: ${ANTHROPIC_MODEL:claude-sonnet-4-5}
        max-tokens: 4096
        timeout-seconds: 60
      openai:
        api-key: ${OPENAI_API_KEY:}
        model: ${OPENAI_MODEL:gpt-4o-mini}
        max-tokens: 4096
        timeout-seconds: 60
      google-ai:
        api-key: ${GOOGLE_AI_API_KEY:}
        model: ${GOOGLE_AI_MODEL:gemini-1.5-flash}
        max-tokens: 4096
        timeout-seconds: 60

    # Embedding service
    embedding:
      enabled: ${EMBEDDING_ENABLED:false}
      openai-api-key: ${OPENAI_API_KEY:}
      model: text-embedding-3-small
      base-url: ${EMBEDDING_BASE_URL:}

    # Content moderation
    moderation:
      enabled: ${MODERATION_ENABLED:true}
      openai-api-key: ${OPENAI_API_KEY:}

# Observability
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 1.0

otel:
  service:
    name: ${spring.application.name}
  exporter:
    otlp:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}

logging:
  level:
    root: INFO
    com.selfcare: DEBUG
  pattern:
    console: "%d{ISO8601} [%thread] %-5level [%X{tenantId:-},%X{correlationId:-}] %logger{36} - %msg%n"
