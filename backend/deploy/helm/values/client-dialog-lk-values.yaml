# dialog-lk tenant overrides (Sri Lanka telco)
# Consumed via: -f backend/deploy/helm/values/client-dialog-lk-values.yaml
tenant:
  id: dialog-lk
  industry: telco
  type: operator
  region: lk

allowlist: "localhost,mock,bss-dialog.svc,smsc-dialog.svc,aia-api.svc"