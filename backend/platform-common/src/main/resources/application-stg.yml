# STAGING profile overrides
selfcare:
  security:
    cors:
      allowed-origins: https://admin.stg.selfcare.io

otel:
  enabled: true

logging:
  level:
    com.selfcare: INFO

spring.jpa.show-sql: false
spring.jpa.properties.hibernate.format_sql: false
