# REGIONAL profile overrides
spring:
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "https://admin.reg.selfcare.io"
            allowedMethods: GET,POST,PUT,DELETE,PATCH,OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

selfcare:
  security:
    jwt:
      jwks-uri: ${CUSTOMER_IDENTITY_URL:http://customer-identity}/.well-known/jwks.json}
  # WAF enabled in regional environment
  waf:
    enabled: true
    log-only-mode: false
    block-on-match: true
  otel:
    enabled: true

logging:
  level:
    com.selfcare: INFO
