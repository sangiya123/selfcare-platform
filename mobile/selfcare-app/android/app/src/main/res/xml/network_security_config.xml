<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration.
    - In debug, allow cleartext traffic to localhost and dev IP ranges.
    - In release, require TLS for everything.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug override: allow cleartext to localhost and common dev IPs -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>  <!-- Android emulator host loopback -->
        <domain includeSubdomains="true">10.0.3.2</domain>  <!-- Genymotion host loopback -->
    </domain-config>
</network-security-config>
