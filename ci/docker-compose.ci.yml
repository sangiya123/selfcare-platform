name: selfcare-ci

services:
  sonarqube:
    image: sonarqube:community
    container_name: selfcare-sonarqube
    restart: unless-stopped
    ports:
      - "9000:9000"
    environment:
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    volumes:
      - sonarqube-data:/opt/sonarqube/data
      - sonarqube-extensions:/opt/sonarqube/extensions
      - sonarqube-logs:/opt/sonarqube/logs
    networks:
      - selfcare-ci

  jenkins:
    build:
      context: ./jenkins
    container_name: selfcare-jenkins
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "50000:50000"
    environment:
      JENKINS_ADMIN_ID: ${JENKINS_ADMIN_ID:-admin}
      JENKINS_ADMIN_PASSWORD: ${JENKINS_ADMIN_PASSWORD:?Set JENKINS_ADMIN_PASSWORD in ci/.env.ci}
      SONAR_TOKEN: ${SONAR_TOKEN:?Set SONAR_TOKEN in ci/.env.ci}
      JAVA_OPTS: >-
        -Djenkins.install.runSetupWizard=false
        -Dcasc.jenkins.config=/var/jenkins_home/casc_configs/jenkins.yaml
    volumes:
      - jenkins-home:/var/jenkins_home
      - jenkins-workspace:/workspace
      - maven-cache:/var/maven-cache
      - //var/run/docker.sock:/var/run/docker.sock
      - type: bind
        source: ${USERPROFILE}/.kube
        target: /var/jenkins_home/.kube
        read_only: true
    depends_on:
      - sonarqube
    networks:
      - selfcare-ci
      - selfcare-net

volumes:
  sonarqube-data:
    name: selfcare-sonarqube-data
  sonarqube-extensions:
    name: selfcare-sonarqube-extensions
  sonarqube-logs:
    name: selfcare-sonarqube-logs
  jenkins-home:
    name: selfcare-jenkins-home
  jenkins-workspace:
    name: selfcare_jenkins_workspace
  maven-cache:
    name: selfcare_maven_cache

networks:
  selfcare-ci:
    name: selfcare-ci
  selfcare-net:
    external: true
